# Orange Lab Microbiology CDSS — مراجعة أغسطس 2026
### Engine-agreement & table-reachability audit — commercial build (`streamlit_app.py`)

**تاريخ المراجعة:** 2026-08-01 · **الإصدار المرجعي:** EUCAST v16.0 · EUCAST Intrinsic v3.3 · EUCAST Expert Rules v3.1 · CLSI M100 Ed36 · CLSI M11 Ed10 · IDSA AMR v4.0 (2024)

**الحجم:** 13 ملف معدّل · 1 ملف جديد · ~840 سطر متغيّر (بدون الـ snapshot)

---

## نقطة البداية

كل الـ suites الموجودة كانت **خضراء**: 86 + 33 + 28 + 63 + 803 سيناريو. العيوب التالية اتلقت بـ adversarial probes جديدة برّه الـ invariants القائمة.

الخيط المشترك في ٦ من ١١: **المنطق موجود وصحيح في مكان واحد، وناقص في المكان التاني اللي بيتنفّذ.** الاختبارات كانت بتثبت إن الكود بيطابق الجداول — مش إن الجداول كاملة، ولا إن المحركين بيتفقوا.

---

## ١. أخطاء تؤثر على المريض مباشرة

### 1.1 — Aztreonam كان يُوصى به في ESBL bacteraemia ⛔
- **الحالة:** `_is_penicillin_or_ceph()` بيطابق tokens: `penicillin · cephalosporin · cillin · cef · ceph`. كلاس Aztreonam هو `"Monobactam (IV)"` — مفيهوش ولا واحد.
- **الأثر:** عزلة *Klebsiella pneumoniae* دم بـ ESBL مؤكّد (ثقة 88%) كانت بتخرج بـ `ALLOWED = ['Aztreonam', 'Amikacin', 'Meropenem']`. الـ terminal safety gate مكانش بيمسكها (`moves: []`).
- **المفارقة:** docstring الدالة المستدعية بيقول حرفياً *"ESBL → resistant to ALL penicillins + cephalosporins **(+ aztreonam)**"*.
- **المصدر:** Aztreonam أوكسيمينو بيتا-لاكتام — الركيزة الكلاسيكية للـ ESBL، و CLSI بيستخدمه كـ screening agent.
- **الإصلاح:** إضافة `monobactam` و `aztreonam` للـ tokens.

### 1.2 — Vancomycin و Linezolid كانوا يُوصى بيهم لكل سالب جرام ⛔
- **الحالة:** `INTRINSIC_RESISTANCE["enterobacterales"]` مكتوب صح وكامل، والـ matcher substring حرفي:
  `"enterobacterales" in "escherichia coli"` = False في الاتجاهين.
- **الأثر:** الصف العائلي كان بيتورّث للسلسلة الحرفية `"Enterobacterales (unspeciated)"` **بس**. كل نوع مُسمّى — E. coli · Klebsiella · Proteus · Salmonella · Stenotrophomonas — مكانش بيورّث حاجة.
- **تناقض بين المحركين:** `ast_qa_engine` كان بيطبع `[CRITICAL] biologically impossible … Do not use for clinical decisions` والعمود المجاور بيحط نفس الدوا في **RECOMMENDED**. مخرجات الـ QC عرض فقط، مفيش feedback loop.
- **الإصلاح:** وراثة تصنيفية في `clinical_data.py` وقت الاستيراد — جدول واحد وقاعدة بحث واحدة، كل المستهلكين بلا تعديل.
- **عضوية المجموعات ضيّقة بقصد:** `_GRAM_POS_ONLY_CORE` لكل سالب جرام؛ الماكروليدات + clindamycin للمعوية واللا-مخمّرة **فقط** — **azithromycin خط أول للتيفويد والشيجيلا والكامبيلوباكتر**، وتطبيق الصف العائلي كله كان هيمنع دوا الاختيار لتلات كائنات.

### 1.3 — Colistin كان يُوصى به لـ Enterococcus و VRE ⛔
- **الحالة:** الجدول بيمنعه صح لـ `staphylococcus aureus` وكل `streptococcus*`، وصفوف `enterococcus faecalis / faecium / spp. / vre` فيها Aztreonam بس.
- **نفس الفجوة في تلات جداول:** `INTRINSIC_ADDENDA` في `clinical_matrix.py` فيه Fusidic acid للـ Enterococcus بدون Colistin.
- **الإصلاح:** `_GRAM_NEG_ONLY_CORE` لكل الصفوف موجبة الجرام + Fusidic acid للمعويات.

### 1.4 — Serratia و Enterobacter بيتحوّلوا لـ E. coli في صمت ⛔
- **الحالة:** ٥ aliases في `ORGANISM_OCR_ALIASES` بتشاور على مفاتيح غير موجودة في `ORGANISM_PROFILE`، و `best_default_index()` بترجّع **0**.
- **الأثر:** تقرير *Serratia marcescens* دم بـ `Ceftriaxone=S` كان بيتحلّل كـ **E. coli** → **Ceftriaxone RECOMMENDED**، من غير أي تحذير derepression. ودي الحالة اللي فيها 8–40% من العزلات بتطوّر مقاومة أثناء العلاج بالجيل الثالث.
- **الجذر البنيوي:** من الـ 11 كائن في `AMPC_PRODUCERS`، **10 كانوا غير قابلين للوصول** من القائمة. مجموعة SPICE كلها dead code من ناحية المستخدم. الصفوف موجودة في `clinical_data` ومفيش طريق يوصلّها.
- **الإصلاح:** ٦ profiles جديدة كاملة بقاعدة الـ derepression: Enterobacter cloacae · Serratia marcescens · Citrobacter freundii · Morganella morganii · Providencia spp. · Hafnia alvei — مربوطة في ٤ طبقات (profile · specimen map · OCR aliases · `_ORG_CANON`).

### 1.5 — Amox-Clav في MRSA: حكمان مختلفان لنفس العزلة ⛔
- **الحالة:** فرع الـ MRSA بيفلتر على `class` لوحده بـ `penicillin / cephalosporin / carbapenem`. كلاس Amox-Clav هو `"Beta-lactamase Inhibitor Combination"` — مفيهوش ولا واحد. باقي الـ BLI combos بتعدّي **بالصدفة** لأن كلاسها بينطق "Penicillin"/"Cephalosporin".
- **الأثر:** `Staphylococcus aureus + Oxacillin-R` → **CAUTION**؛ نفس العزلة مكتوبة `MRSA` → **BANNED**. الحكم بيتغيّر حسب صياغة المحلل للاسم.
- **المصدر:** mecA/PBP2a هدف مُعدَّل مش beta-lactamase — الكلافولانيت ملوش أي دور.
- **الإصلاح:** المطابقة بقت على `_cls_and_name()` + `*_BLI_TOKENS`.

### 1.6 — نتيجة `I` كانت تُمحى عند القصور الكلوي أو الكبدي ⛔
- **الحالة:** `warning_reason` خانة واحدة، وفروع الـ hepatic والـ renal بتعمل `continue` **قبل** فحص `culture_result == "I"`.
- **الأثر:** `Ciprofloxacin=I` + `CrCl 25` → `warning_reason = 'renal_adjustment'`، والبانر المخصص للـ Intermediate بيفلتر على `warning_reason` فبيختفي.
- **الأخطر:** التعليمتان **متعاكستان**. EUCAST بتعرّف `I` بأنها *Susceptible, Increased exposure* — **زوّد** الجرعة؛ والـ renal note بيقول **قلّل**. المستخدم كان بيشوف واحدة بس.
- **الإصلاح:** مفتاح `culture_intermediate` مستقل بيتختم بعد اللوب. **إعادة الترتيب اترفضت** — الترتيب load-bearing (شوف كومنت بلوك الحمل) وتحريكه بيخاطر بإن caution تسبق hard ban. + تحذير صريح بالتعارض في التلات renderers.

---

## ٢. صمت في محرك الجودة (QC silences)

`ast_reportability.INTRINSIC_RULES` **قائمة مكتوبة بإيد، مش إسقاط من `clinical_data`**. كل صف جديد في الجدول بيحتاج قاعدة يدوية مقابلة. الكومنتات في السطور 125 و 418 و 499 بتوثّق نفس المشكلة حصلت تلات مرات قبل كده.

| القاعدة المضافة | الصمت اللي كانت بتغطيه |
|---|---|
| `intr_anaerobes_aminoglycosides_polymyxins` | اللاهوائيات مكانش ليها **أي صف** — `Gentamicin S` و `Colistin S` كانوا بيوصلوا RECOMMENDED |
| `intr_haemophilus_gram_pos_agents` | H. influenzae مكانش ليه صف — `Vancomycin S` غير مُبلَّغ عنه (الماكروليدات **مستثناة** بقصد) |
| `intr_vre_vancomycin_contradiction` | `VRE + Vancomycin=S` تناقض مباشر بين الـ ID والـ AST — المحرك بيرفض والـ QC ساكت |

التلاتة معاهم citation rows في `guideline_registry.py`.

**ملاحظة تستاهل قرار منفصل:** المحركين بيرجّعوا **schemas مختلفة** — `ast_qa_engine` بيرجّع `QAIssue` dataclass بـ `.message`/`.drug` (مفرد)، و `ast_reportability` بيرجّع dicts بـ `drugs` (جمع)/`reason_en`. قراية الغلط منهم بترجّع سلسلة فاضية وكل فحص بيعدّي في صمت.

---

## ٣. عيوب بنيوية

### 3.1 — تلات suites مكانوش شغالين في الـ CI
`test_safety_invariants.py` (28 proof) · `test_clinical_matrix.py` (33 proof) · `test_guidelines.py` — مذكورين في الـ README ومش في الـ workflow. يعني **الـ terminal safety gate**، أحدث وأخطر طبقة، كان بيشحن بلا حراسة على كل push.
- **الإصلاح:** Guards 6–9 + **Guard 10** بيلف على كل `test_*.py` ويفشل الـ build لو أي suite مش مذكور — عشان الحالة دي ما تتكررش.

### 3.2 — عزلة بلا اسم كانت تُنتج "AmpC 75%"
`is_esbl_producer("")` بترجّع **True** — `"" in "escherichia coli"` صحيحة ومفيش length guard. `_remove_intrinsic_resistance()` و `is_intrinsically_avoided()` **الاتنين عندهم الحارس ده** (`len >= 4`) وموثّق بكومنت بيشرح نفس الخطر. اتكتب مرتين واتنسي في التالت.
- **الإصلاح:** helper مشترك `_org_matches()` + `_ORG_NON_INFORMATIVE` (توكنات زي `"spp."` بتعدّي حاجز الـ 4 حروف وهي مش أسماء) + `(organism or "")` في `predict_esbl` اللي كان بيرمي `AttributeError`.

---

## ٤. الـ OCR

### 4.1 — أكواد الأقراص مكانتش مقروءة → MDR بيتفوّت
`ABX_ALIAS_INDEX` فيه 176 alias غني بالأسماء التجارية المصرية (Augmentin · Curam · Unictam · Sigmaclav · Unasyn · Tazocin) و**صفر** أكواد CLSI/EUCAST. أجهزة VITEK و Phoenix والأقراص اليدوية كلها بتطبع بالكود.

- **الأثر المقاس:** نفس عزلة *Pseudomonas*: تقرير كامل → **MDR (4/5 فئات)**؛ نفس التقرير بأكواد → **level=None**. التنبيه اختفى تماماً ومفيش إشارة إن نص اللوحة مقروش.
- **ليه مش مجرد aliases:** أكواد زي `P` · `E` · `DO` · `CN` · `TE` من حرف أو حرفين، و `normalize_abx_key` بيشيل الترقيم — فـ `"Patient"` فيها `p` و `"Date"` فيها `te`. كلهم كانوا هيولّعوا على ترويسة التقرير.
- **الإصلاح:** الكود بيُحترم **فقط** لما يكون توكن كامل قائم بذاته **و** السطر فيه verdict S/I/R. الأسماء الكاملة لها الأولوية دايماً.

```
'AMC S / CIP R / SXT S / MEM S'  → 4 أدوية      ✅
'P R / E S / DA S / VA S'         → 4 أدوية      ✅
'Patient Name: Ahmed  Date: ...'  → []           ✅
'The AMC result was discussed'    → []           ✅
'Ciprofloxacin  CIP  R'           → دوا واحد     ✅
```

---

## ٥. جدول التفاعلات الدوائية

المحرك سليم؛ المحتوى كان ناقص.

| الدوا | قبل | بعد |
|---|---|---|
| **Clarithromycin** | `[]` فاضي | 7 — أهمها **colchicine** (سمية قاتلة، contraindication مطلق في الـ EU label) · statins · digoxin · CCBs · QT · ergot |
| **TMP-SMX** | Warfarin فقط | 8 — **methotrexate** (مضادا فولات → قلة كريات شاملة) · **ACEi/ARB/spironolactone** (الـ trimethoprim بيسدّ قناة الصوديوم النبيبية زي الأميلورايد → فرط بوتاسيوم، سبب موثّق للوفاة المفاجئة في المسنين) · phenytoin · sulfonylureas · azathioprine |
| **Linezolid** | SSRI فقط | 5 — MAOI · pethidine/tramadol · sympathomimetics · tyramine |

Erythromycin كان عنده مدخل الـ statins، و clarithromycin — الأقوى منه كمثبط CYP3A4 — كان فاضي.

**وده كان لازمه إصلاح تاني:** `"ACE inhibitors"` مكتوبة كـ class، بس حد بيكتب **Lisinopril**. `_MED_CANON` اتوسّع بالأسماء الجنيسية عشان الـ class يبقى قابل للوصول منها.

---

## ٦. الأمان

| العيب | الإصلاح |
|---|---|
| `elif SUBSCRIBER_HASHES:` كان بيطبع تحذير **ويكمّل ويرجّع True** — الكومنت جنبه بيقول *"do not silently accept a bare email while the rest of the estate is protected"*، واللي الكود كان بيعمله بالظبط ناقص الصمت. أي حساب في `subscribers` بدون hash = باب بلا كلمة مرور. | `return False` — أول ما أي حساب يتحمى، الحساب اللي بلا hash مبيدخلش خالص |
| مفيش أي حد لمحاولات كلمة المرور — `logger.warning` بس | 5 محاولات → قفل 5 دقايق، بيتصفّر عند النجاح |

---

## ٧. الاختبارات

### ملف جديد: `test_engine_agreement.py` (12 فحص)

الـ suites القائمة بتثبت إن **الكود بيطابق الجداول**. الملف ده بيثبت إن **محركين بيبصّوا لنفس العزلة بيوصلوا لنفس الحكم**، وإن **صف موجود في جدول بيتورّث لحد فعلاً**.

كل فحص بيوثّق العيب اللي بيحرسه، فاللي هيقع فيه بعدين يعرف الرهان كان إيه.

### الحالة النهائية

```
9/9 suites PASS  (بدون streamlit مثبّت، زي الـ CI)
1184 سيناريو · 115 زوج (كائن × عينة) · كل الـ 15 invariant
Guard 10: كل الـ suites موصولة بالـ CI
```

### مراجعة الـ snapshot

الـ snapshot اتحدّث **مرتين، وكل مرة بعد تحقق آلي إنه شدّ ومفكّش**:

| الجولة | حالات جديدة | سطور اتشدّت | **loosenings** |
|---|---|---|---|
| الأولى | 6 | 75 | **0** |
| الثانية | 159 | 24 | **0** |
| الثالثة | 154 | 28 | **0** |

ولا حالة واحدة في التلات جولات فتحت خيار جديد.

---

## ٨. ملاحظات مفتوحة

1. **`ast_reportability` مصدر حقيقة تالت.** القواعد مكتوبة بإيد ومش مولّدة من `clinical_data`. الـ check رقم [9] في الـ suite الجديدة بيمسك الصمت الناتج، بس الحل الحقيقي refactor.
2. **`modules/` · `ui/` · `data/` · `qc.py` · `antibiotics.py`** منفصلين تماماً عن `streamlit_app.py` — ~2,600 سطر منطق سريري موازي مغطّى بـ `compileall` بس. `modules/mdr.py` لسه فيه نسخة قديمة من `predict_esbl` بدون intrinsic stripping وبقائمة `ESBL_PRODUCERS` من 5 كائنات بدل 18. لو مش مستخدمين، يتشالوا — أو `raise ImportError` في `modules/__init__.py`.
3. **`classify_specimen` بيرجّع `""` للمجهول (fail-open) و `canonical_site` بيرجّع `None` (fail-closed).** عقيدتان مختلفتان لنفس السؤال. مش خطير حالياً لأن الـ selectbox ثابت.
4. **39 قاعدة لسه مستنية توقيع طبيب** في `guideline_registry.py` + 16 صف جرعة في `test_dose_adjustment --queue`. المراجعة دي مغيّرتش الوضع ده.
